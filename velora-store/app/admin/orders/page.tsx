import { orders } from '@/lib/data';import { Shell } from '@/lib/ui';export default function AdminOrders(){return <Shell><div className="py-10"><h1 className="text-4xl font-black">Заказы</h1><div className="mt-6">{orders.map(o=><div className="card p-4 mb-3 flex justify-between"><span>#{o.id} · {o.client}</span><b>{o.status}</b></div>)}</div></div></Shell>}
