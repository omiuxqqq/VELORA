import { PageShell } from '@/lib/components'
import { reviews } from '@/lib/data'
export default function Reviews(){return <PageShell><main className="container section"><h1>Отзывы покупателей</h1><div className="grid cols3">{reviews.map(r=><div className="card" key={r.name}><div className="stars">★★★★★</div><h3>{r.name}</h3><p className="muted">{r.product}</p><p>{r.text}</p></div>)}</div><section className="card section" style={{marginTop:24}}><h2>Оставить отзыв</h2><form className="form"><input className="input" placeholder="Ваше имя"/><input className="input" placeholder="Товар"/><textarea className="input" placeholder="Ваш отзыв" rows={4}/><button className="btn" type="button">Отправить отзыв</button></form><p className="muted">После подключения Supabase отзывы будут сохраняться и проходить модерацию.</p></section></main></PageShell>}
